# requestman/node

Node.js rewrite of the Requestman app.

## Requirements

- Node.js 20+
- Yarn

## Commands

Install dependencies:

```bash
yarn install
```

Build the frontend bundle:

```bash
yarn build
```

Run the app:

```bash
node server.js
```

Run in production mode:

```bash
yarn start
```
